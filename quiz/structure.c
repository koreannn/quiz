#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int Sum(int *p, int num)
{
	for (int i = 0; i < num; i++)
	{

	}
}

int main()
{
	int n = 0;
	printf("입력할 정수의 개수는? : ");
	scanf("%d", &n);
	int* arr = (int*)malloc(sizeof(int) * n);
	printf("\n정수를 입력하세요 : \n");





	
	return 0;
}




//int k = 0, q = 0, l = 0, b = 0, n = 0, p = 0;
//printf("킹, 퀸, 룩, 비숍, 나이트, 폰의 개수를 각각 입력 : \n");
//scnaf("%d%d%d%d%d%d", &k, &q, &l, &b, &n, &p);

/*
char id[50], inputid[50];
int count = 0;
printf("이미 존재하는 아이디를 입력 : ");
gets(id);
printf("입력할 아이디를 입력 : ");
gets(inputid);
while (inputid[count] == '\0')
{
	if (id[count] = !inputid[count])
		return 0;


	count++;
}
printf("??!");
*/