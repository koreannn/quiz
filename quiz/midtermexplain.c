/*#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>

int main()
{
	/*
	* <<19번, 메모리 동적할당 문제>>

	int nS = 0;
	int* score = NULL;

	scanf("%d", &nS);
	score = (int*)malloc(nS * sizeof(int));

	for (int i = 0; i < nS; i++)
	{
		scanf("%d", &score[i]); //scanf("%d", score+i)와 동일
	}

	for (int i = 0; i < nS; i++)
	{
		printf("%d\n", score[i]); //printf("%d\n", *(score+i));
	}
	free(score);
	*/

	//블랙잭 문제는 e참뜰 참고



	//return 0;
//}